{
  "name": "versatiles-choro",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "check": "npm run check -ws",
    "cli": "npm run cli -w packages/backend",
    "dev": "npm run dev -w packages/webapp",
    "upgrade": "ncu --workspaces"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "npm-check-updates": "^19.1.2"
  }
}