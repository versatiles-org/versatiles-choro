/**
 * Server-specific test setup
 * Configuration for server-side tests
 */

import { vi } from 'vitest';

// Mock any server-specific modules if needed
// Currently minimal as server tests run in Node environment
