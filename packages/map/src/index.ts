export function buildMap(mount: HTMLElement, config: string) {
	console.log('buildMap called with config:', config);
	mount.innerHTML = '<div style="padding:1em;color:#666">[Map Placeholder]</div>';
}
